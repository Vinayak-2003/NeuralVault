# 📁 NeuralVault Backend — Folder Structure Map

> **Generated:** 2026-06-15 | **Purpose:** Quick reference for onboarding, debugging, and code reviews.

---

## Root Directory

```
Backend/                                 # Project root
│
├── main.py                    (1,376 B) # FastAPI app entry point — lifespan, router registration, CORS
├── pyproject.toml               (716 B) # uv project metadata + dependency list
├── requirements.txt             (360 B) # pip-compatible dependency mirror (subset of pyproject.toml)
├── uv.lock                  (761,969 B) # Locked dependency tree (auto-generated by uv)
├── alembic.ini                (5,028 B) # Alembic migration runner configuration
├── .env.example               (1,371 B) # Template for environment variables
├── .env                         (750 B) # ⛔ SECRETS — never committed (gitignored)
├── .python-version                (5 B) # Pins Python 3.12
├── .gitignore                   (203 B) # Git exclusion rules
├── __init__.py                    (0 B) # Makes Backend/ importable (unnecessary at root)
├── README.md                 (18,158 B) # Project documentation
│
├── .venv/                               # Virtual environment (gitignored)
├── __pycache__/                         # Bytecode cache (gitignored)
│
├── alembic/                             # Database migration framework
│   ├── env.py                 (2,224 B) #   Migration runner config — imports Base, sets DB URL
│   ├── script.py.mako           (704 B) #   Jinja2 template for new migration files
│   ├── README                    (38 B) #   Alembic placeholder readme
│   └── versions/                        #   Auto-generated migration scripts
│       ├── ffc52e789069_create_document_table.py               (749 B)
│       ├── 44756904bd56_create_document_table.py             (1,549 B)
│       ├── 73e56b6a0479_updated_job_id_and_uploaded_at_time.py (1,509 B)
│       ├── b24c0f5809f6_created_config_class.py              (2,039 B)
│       ├── 7f3b3a02ddc3_removed_chunk_size_and_overlap_config_.py (1,092 B)
│       ├── 7d887d6ff539_added_is_active_field_in_config_class.py  (901 B)
│       └── 8745a0e998b8_updated_documentstatus_enums.py        (898 B)
│
└── app/                                 # ★ Application source code
    ├── __init__.py                (0 B) #   Package marker
    │
    ├── api/                             # ── HTTP Layer ──
    │   ├── __init__.py            (0 B)
    │   └── routes/                      #   Route handlers (one file per domain)
    │       ├── __init__.py        (0 B)
    │       ├── health.py        (175 B) #     GET /health — liveness probe
    │       ├── document.py      (752 B) #     GET /documents, DELETE /documents/{doc_id}
    │       ├── ingest.py      (1,194 B) #     POST /ingest, GET /ingest/status/{job_id}
    │       ├── query.py         (701 B) #     POST /query
    │       ├── config.py        (780 B) #     GET/POST /config, POST /config/default
    │       └── setting.py     (1,637 B) #     GET/POST /config (DUPLICATE — async variant, unused)
    │
    ├── core/                            # ── Configuration ──
    │   ├── __init__.py            (0 B)
    │   └── config.py          (1,410 B) #     Pydantic Settings — loads all env vars
    │
    ├── db/                              # ── Data Storage ──
    │   ├── __init__.py            (0 B)
    │   ├── database.py        (1,439 B) #     SQLAlchemy engine, session factory, Base declarative
    │   ├── docs/                        #     Uploaded PDF file storage (17 files, ~22 MB)
    │   │   ├── *.pdf                    #       (resumes, Azure docs, LangChain, Python, etc.)
    │   ├── chroma_db/                   #     ChromaDB persistent vector store directory
    │   └── bm25_retriever.pkl (1,143 KB)#     Pickle-serialised BM25 keyword index
    │
    ├── models/                          # ── Pydantic Models (request/response DTOs) ──
    │   ├── __init__.py            (0 B)
    │   ├── query_model.py       (122 B) #     QueryModel: query, top_k, doc_filter
    │   ├── config_model.py      (518 B) #     BaseConfig, FetchConfig + SearchCategory enum
    │   ├── document_model.py    (597 B) #     DocumentModel + DocumentStatus enum
    │   └── setting_model.py     (960 B) #     Setting SQLAlchemy ORM (⚠️ misplaced — should be in schemas/)
    │
    ├── schemas/                         # ── SQLAlchemy ORM Models (DB tables) ──
    │   ├── __init__.py            (0 B)
    │   ├── document_schema.py (1,037 B) #     Document table + DocumentStatus enum
    │   ├── config_schema.py   (1,217 B) #     RAGConfig table + SearchCategory enum
    │   └── setting_schema.py    (553 B) #     SettingCreate/SettingResponse Pydantic (⚠️ misplaced)
    │
    ├── rag/                             # ── RAG Pipeline Core ──
    │   ├── __init__.py            (0 B)
    │   ├── ingestion_pipeline.py (5,101 B) #  PDF load → split → enrich → embed → store
    │   ├── retrieval_pipeline.py   (693 B) #  Qdrant hybrid similarity search
    │   ├── llm_service.py       (2,076 B) #  Prompt template + Groq LLM invocation
    │   ├── bm25_retriever.py    (1,294 B) #  BM25RetrieverClass — pickle-based keyword index
    │   └── metadata_enricher.py (1,049 B) #  Enrich chunk metadata with doc_id, filename, etc.
    │
    ├── services/                        # ── Business Logic Layer ──
    │   ├── __init__.py            (0 B)
    │   ├── ingestion_service.py (2,204 B) #  Orchestrates file save + ingestion pipeline
    │   ├── retrieval_service.py   (704 B) #  Calls retriever → LLM → returns answer
    │   ├── document_service.py  (1,271 B) #  DB queries for documents list & deletion
    │   ├── metadata_service.py  (2,722 B) #  DB writes for document status tracking
    │   └── config_service.py    (1,532 B) #  RAG config CRUD operations
    │
    └── utils/                           # ── Shared Utilities ──
        ├── __init__.py            (0 B)
        ├── embeddings.py        (231 B) #     HuggingFace BGE-small-en embedding factory
        ├── chroma_client.py     (538 B) #     ChromaDB client factory (⚠️ legacy, partially unused)
        ├── qdrant_client.py   (1,589 B) #     Qdrant hybrid vector store factory
        └── save_file.py         (836 B) #     Async file save utility for uploads
```

---

## File Count Summary

| Layer          | Files | Purpose                                    |
|----------------|-------|--------------------------------------------|
| Root config    | 8     | Entry point, deps, env, alembic, gitignore |
| API routes     | 6     | HTTP endpoint handlers                     |
| Core           | 1     | Centralized Pydantic Settings              |
| DB             | 1     | SQLAlchemy engine + session                |
| Models (DTOs)  | 4     | Pydantic request/response schemas          |
| Schemas (ORM)  | 3     | SQLAlchemy table definitions               |
| RAG pipeline   | 5     | Ingestion, retrieval, LLM, BM25, metadata  |
| Services       | 5     | Business logic orchestration               |
| Utils          | 4     | Embeddings, vector stores, file I/O        |
| Alembic        | 8     | Migration scripts + env                    |
| **Total**      | **45**| (excluding __init__.py, __pycache__, .venv)|

---

## Data Files (Non-Code)

| File                         | Size      | Location         | Purpose                    |
|------------------------------|-----------|------------------|----------------------------|
| `bm25_retriever.pkl`         | 1.14 MB   | `app/db/`        | Serialised BM25 index      |
| `chroma_db/`                 | Variable  | `app/db/`        | ChromaDB vector embeddings |
| `docs/*.pdf`                 | ~22 MB    | `app/db/docs/`   | Uploaded PDF documents     |
| `uv.lock`                    | 762 KB    | Root             | Locked dependency tree     |
